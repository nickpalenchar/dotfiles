- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.zsh: zsh
    ~/.zsh-syntax-highlighting: zsh-syntax-highlighting
    ~/.alacritty.yml: alacritty.yml
    ~/.zpreztorc: zpreztorc
